/backend/vortex/api.py
- setup tastypie api to receive create requests
- tell tastypie to use json by default and not require ?format=json
- filter conversations by ones request.user is a council member of, and authorization

/backend/vortex/models.py
- is CouncilMember necessary? why not just give Conversation many council_members?

/frontend/spirit/www/scripts/templates/app/conversation.mustache
- use mustache to detect existence of author inside text object

/frontend/spirit/www/scripts/views/app/conversation.js
- unbind events when leaving a ConversationView so when one is created again single events don't get fired multiple times